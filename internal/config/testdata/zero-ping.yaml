server:
  port: 8080
  tls:
    enabled: false
credentials_file: "./credentials.yaml"
registry_file: "./registry.json"
websocket:
  ping_interval_seconds: 0
logging:
  format: json
  level: info
cors:
  allowed_origins: []
