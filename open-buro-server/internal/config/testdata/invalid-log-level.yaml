server:
  port: 8080
  tls:
    enabled: false
credentials_file: "./credentials.yaml"
registry_file: "./registry.json"
websocket:
  ping_interval_seconds: 30
logging:
  format: json
  level: verbose
cors:
  allowed_origins: []
